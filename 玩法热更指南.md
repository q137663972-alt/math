# 新增玩法 · 热更操作指南

> 结论先行：**新增玩法不用出新 APK。**
> 写一个 `js/game-xxx.js`，跑一次 `node tools/gen-pack.mjs`，推上 GitHub，用户下次打开 App 就看到新玩法了。

---

## 1. 热更覆盖矩阵

| 内容 | 能否热更 | 做法 |
|---|---|---|
| 课文 / 单词 / 题目数据（`js/data-*.js`） | ✅ | 改文件 → `gen-pack` |
| 改现有玩法逻辑（`js/games.js` 里的 `startXxx`） | ✅ | 改文件 → `gen-pack` |
| **新增玩法（新文件）** | ✅ | 新建 `js/game-*.js` → `registerGame` → `gen-pack` |
| 图片 / 音频 / 字体（`img/`、`audio/`、`font/`） | ✅ | 放文件 → `gen-pack`（走 `assets.zip`，**无 5MB 限制**） |
| 样式（`css/style.css`） | ✅ | 改文件 → `gen-pack` |
| 首页文案、单元结构、星星规则（`js/app.js`） | ✅ | 改文件 → `gen-pack` |
| **`js/boot.js`（热更引导器本身）** | ❌ | 必须出新 APK |
| 原生能力（新增 JS 桥、权限、语音识别语种、SDK 版本） | ❌ | 必须出新 APK |
| 应用图标 / 包名 / versionCode / 启动页 | ❌ | 必须出新 APK |

> `boot.js` 里标了「冻结文件」。它是从内置 assets 里加载的，永远不会从热更包里替换 ——
> 这条设计保证了：热更的 js 再怎么坏，也毁不掉「回退到内置版本」这条逃生通道。

---

## 2. 新增玩法：三步走

### 第 1 步：新建 `js/game-xxx.js`

```js
/* 玩法：xxx */
(function () {
  function startXxx() {
    var u = curUnit();
    // …出题、渲染、判分，照抄 games.js 里任一玩法的写法即可…
    gameShell('<div class="big-emoji">🎯</div><div class="prompt">题干</div>');
  }

  /* 这一行是热更的关键：id 会被 gen-pack.mjs 扫进资源包清单 */
  registerGame({
    id: "xxx",            // 唯一 id，与首页入口一一对应
    name: "玩法名",        // 首页卡片标题
    icon: "🎯",            // 首页卡片图标（emoji 即可）
    desc: "一句话说明",     // 首页卡片副标题
    start: startXxx        // 点进去执行的函数
  });
})();
```

命名规则只有两条：
- 文件必须叫 `js/game-*.js`（生成器按这个前缀扫描）
- 文件里必须有 `registerGame({ id:"…" })`（生成器从这行读 id）

### 第 2 步：生成资源包

```bash
node tools/gen-pack.mjs
```

输出里会列出扫到的玩法：

```
✅ 资源包已生成 → hot/pack   (app=english min_apk=5)
   build     7360f15588786cd5
   代码文件  14 个  → code.zip   47.3KB
   玩法      xxx
```

### 第 3 步：提交推送

```bash
git add -A && git commit -m "新增玩法：xxx" && git push
```

CI 会自动把 `hot/pack/` 发布到 GitHub Pages。用户端：打开 App → 启动成功 3 秒后后台静默下载 → **下次打开生效**（首页多出一张卡片）。

---

## 3. 它为什么能不用出 APK

```
APK 内置 assets                ← 第 1 层：永远可用，无网也能玩
  ↓
files/hot/（上次装的资源包）    ← 第 2 层：纯本地读取，启动零网络
  ↓
hot/pack/（GitHub Pages）      ← 第 3 层：启动成功 3 秒后才联网
```

- 资源包是 **zip**，由原生桥 `AndroidHot.installPack()` 落盘到 `files/hot/`，
  再用虚拟域 `https://local.hot/` 读取 —— 不经过 localStorage，**没有 5MB 配额限制**，图片音频随便加。
- 玩法文件在清单里单独标记（`games` 数组），`boot.js` 把它们**插在 `js/games.js` 之后、`js/app.js` 之前**加载，
  所以首页第一次渲染就能看到新玩法。这段逻辑已经写死在冻结的 `boot.js` 里 —— 以后加玩法**永远不用再动 `boot.js`**。

### 四道保险（热更出问题也不会白屏）

| 保险 | 行为 |
|---|---|
| 启动路径零网络 | 网络只发生在启动成功后的后台，失败最多是「这次没更新上」 |
| 1.5 秒哨兵 | 没渲染出 `#app` 就判失败 → 标记坏包 → 回滚 → 重载 |
| 连续 2 次失败 | 永久关闭热更，退回内置版本 |
| 逃生开关 | 访问 `index.html#nohot` 或 `index.html?safe=1` 强制走内置 |

调试：设置面板连点标题 5 次 → 打开 `#hotlog` 看完整启动日志，不用 adb。

---

## 4. 发布一版热更的完整命令

```bash
# 1. 改内容（数据 / 玩法 / 图片 / 样式，随便改）
# 2. 生成资源包
node tools/gen-pack.mjs

# 3. 同步进 Android 壳 assets（只有要出新 APK 时才需要）
./sync-assets.sh --apply

# 4. 出新 APK（改了 boot.js / 原生代码 / 版本号时才需要）
./build-apk.sh

# 5. 推送
git add -A && git commit -m "说明" && git push
```

---

## 5. 常见疑问

**Q：改了 `js/games.js` 里的一个玩法，也要走这套吗？**
要，但只需 `node tools/gen-pack.mjs`（不用新建文件）。现有玩法已经在 `games.js` 末尾通过 `registerGame` 注册，改逻辑照样热更。

**Q：用户什么时候看到新内容？**
不是即时。App 打开 → 启动成功 3 秒后后台下载 → **下一次打开**才生效。这是刻意的：绝不让网络影响本次启动。

**Q：能强制立刻生效吗？**
真机上把 App 切到后台再切回来（或杀掉重开）即可触发下一轮启动。

**Q：万一新玩法把 App 搞崩了？**
哨兵 1.5 秒内没渲染出首页 → 自动标记这个 build 为坏包 → 回滚到内置版本 → 重载。用户只会感觉到闪一下，不会白屏。

**Q：热更包有大小限制吗？**
没有硬限制。代码包（js+css）通常几十 KB，资源包（图片音频）单独一个 zip，几 MB 也照下。
